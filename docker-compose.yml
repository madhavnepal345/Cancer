services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/data:/app/backend/data
      - hf-cache:/cache/huggingface

volumes:
  hf-cache:
